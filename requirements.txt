# Deployment dependencies — pinned to tested versions (audit fix, 19 Sep 2026)
# Update and re-test before bumping any version in production.

fastapi==0.141.1
uvicorn==0.52.4
python-multipart==0.0.32
pydantic==2.13.5
lxml==6.1.3
python-docx==1.2.0
httpx==0.28.1

# Auth / config — stable APIs; lower-bound pinned
PyJWT>=2.8.0,<3.0
python-dotenv>=1.0.0,<2.0
PyYAML>=6.0,<7.0
