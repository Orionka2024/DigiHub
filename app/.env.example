# Documentation only: start.sh does not automatically load this file.
# Export desired variables in the process environment. Never put keys here.
KVK_SUBMISSION_PROVIDER=
# Leave unset until a complete filing validator has been qualified.
# KVK_VALIDATOR=your_package.validator:validate_report
# KVK_TAXONOMY_DIR=/absolute/path/to/taxonomies
# There is no production submission switch in this preparation build.
